O código de retorno dos comandos ficam na variável PIPESTATUS, nesse caso a sintáxe seria utilizar um "${PIPESTATUS}" para cada comando a ser executado.

Ex: echo ${PIPESTATUS[0]} ${PIPESTATUS[1]} ${PIPESTATUS[2]} ${PIPESTATUS[3]}
